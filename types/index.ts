export type RegisterRequest = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type Users = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
  avatar: string;
};
